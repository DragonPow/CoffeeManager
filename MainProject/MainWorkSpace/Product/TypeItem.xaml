<UserControl x:Class="MainProject.MainWorkSpace.Product.TypeItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:MainProject.MainWorkSpace.Product"
             mc:Ignorable="d" 
             Height="60" Width="350" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Gray" BorderThickness="1" Grid.ColumnSpan="2"></Border>
        <materialDesign:PackIcon Kind="SquareRounded" Width="30" Height="30" VerticalAlignment="Center" Margin="10,0" Foreground="{StaticResource MainColor}"/>
        <TextBlock Text="{Binding Type , FallbackValue='Nước uống'}"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Center"
                       Grid.Column="1"
                       Foreground="#BD8722"
                       FontSize="20"
                       Margin="10,0"
                       FontWeight="DemiBold" />
    </Grid>
</UserControl>
